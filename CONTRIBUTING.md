# Git 브랜치 및 PR 정책

이 저장소는 AI 서브에이전트(Claude Code)를 활용해 개발하는 프로젝트로, 아래 정책은 AI가 생성한 코드도 예외 없이 사람이 작성한 코드와 동일한 절차를 거치도록 하기 위한 것이다.

## 브랜치 전략

- `main`은 보호된 브랜치다. 항상 배포/심사 가능한 상태를 유지하며, **직접 push는 금지**된다.
- 모든 작업은 브랜치를 만들어 진행하고 Pull Request(PR)로 `main`에 병합한다.
- 브랜치 명명 규칙:
  - `feature/<설명>` — 신규 기능
  - `fix/<설명>` — 버그 수정
  - `docs/<설명>` — 문서만 변경
  - `refactor/<설명>` — 동작 변경 없는 리팩터링
  - A-SPICE 프로세스 단계를 명시하고 싶으면 `swe1/`, `swe2/`, `swe3/`, `swe5/` 등 접두어를 붙여도 된다 (예: `swe3/sensor-fusion-detailed-design`).

## PR 규칙

- PR을 열거나 갱신하면 `.github/workflows/ci.yml`(GitHub Actions)이 자동 실행되어 CLAUDE.md의 품질·커버리지 게이트(함수 라인수/순환복잡도/중복코드/주석비율/명명규칙/Branch 커버리지 100%)를 검증한다.
- **CI(필수 상태 검사)를 통과해야 병합할 수 있다.**
- 최소 1인의 리뷰 승인을 권장한다. 리뷰어가 없는 단독 작업 상황에서는 저장소 관리자 권한으로 승인 없이 병합할 수 있도록 branch protection이 설정되어 있다(`enforce_admins: false`) — 다만 이 경우에도 CI 통과는 우회할 수 없다.
- 병합 방식은 **Squash and merge**를 기본으로 하고, 병합 후 브랜치를 삭제한다.
- `main`에 대한 force-push와 브랜치 삭제는 금지된다.

## AI 생성 코드에 대한 추가 규칙

- 서브에이전트가 생성한 코드도 예외 없이 위 PR/CI 절차를 통과해야 한다 — "AI가 만들었으니 검토를 생략한다"는 허용하지 않는다.
- PR 설명에 다음을 명시한다: 어떤 서브에이전트/스킬을 사용했는지, CLAUDE.md 기준 어느 단계(분석/설계/구현/단위테스트/통합테스트/시스템테스트)의 산출물인지, 관련 요구사항/설계 ID.
- CI가 실패한 상태로 병합하지 않는다. 실패 원인을 해결하거나, 정말 예외가 필요하면(예: 도구 자체의 한계) 그 사유를 PR에 기록하고 저장소 관리자의 명시적 승인을 받는다.
- CI 워크플로 자체(`.github/workflows/*.yml`)를 수정하는 PR은 특히 신중히 검토한다 — 게이트를 우회하기 위한 임의 완화는 금지한다.

## 커밋 메시지

- 무엇을 왜 바꿨는지 한 줄로 요약하고, 필요하면 본문에 근거(요구사항 ID, 설계 ID, 이슈 번호)를 남긴다.
- 사소한 스타일 규칙을 강제하지는 않지만, 하나의 커밋은 하나의 논리적 변경 단위로 유지한다.
